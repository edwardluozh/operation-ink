/**
 * IGI samples have been removed for licensing reasons.
 * This file now exports empty objects; the game falls back to CC0/CC BY samples
 * and procedural Web Audio synthesis as documented in public/sounds/CREDITS.md.
 * 
 * Original IGI recordings are not licensed for reuse outside the original game.
 */

export const IGI_SAMPLES: Record<string, { files: string[]; gain: number }> = {}

/** Character vocals removed; all dialogue is caption-only with no audio fallback. */
export const IGI_VOICES: Record<string, string[]> = {}
